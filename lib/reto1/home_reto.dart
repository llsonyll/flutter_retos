import 'package:flutter/material.dart';

class HomeReto extends StatelessWidget {
  const HomeReto({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}

import 'package:flutter/material.dart';

class MainQrScanner extends StatelessWidget {
  const MainQrScanner({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('QRscanner'),
      ),
    );
  }
}

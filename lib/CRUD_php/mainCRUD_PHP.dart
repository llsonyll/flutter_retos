import 'package:flutter/material.dart';

class MainCRUDphp extends StatelessWidget {
  const MainCRUDphp({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CRUD > PHP database'),
      ),
    );
  }
}
